import 'package:flutter/material.dart';

class TvDiscoverPage extends StatelessWidget {
  const TvDiscoverPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("Discover");
  }
}
