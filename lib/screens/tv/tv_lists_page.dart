import 'package:flutter/material.dart';

class TvListsPage extends StatelessWidget {
  const TvListsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("Lists");
  }
}
