import 'package:flutter/material.dart';

class TorrentLoadingPage extends StatelessWidget {
  const TorrentLoadingPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text("Torrent file is initializing...."),
      ],
    );
  }
}
