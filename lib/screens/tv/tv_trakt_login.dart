import 'package:flutter/material.dart';

class TvTraktLogin extends StatelessWidget {
  const TvTraktLogin({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
